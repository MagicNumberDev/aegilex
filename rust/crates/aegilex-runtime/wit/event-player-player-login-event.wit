package aegilex:endstone@0.1.0;

// Endstone header: endstone/event/player/player_login_event.h
interface player-login-event {
    use player.{player};
    use types.{host-error};

    resource player-login-event { get-player: func() -> result<player, host-error>; get-kick-message: func() -> result<string, host-error>; set-kick-message: func(message: string) -> result<_, host-error>; is-cancelled: func() -> result<bool, host-error>; set-cancelled: func(cancelled: bool) -> result<_, host-error>; }
}
