package aegilex:endstone@0.1.0;

// Endstone header: endstone/event/block/leaves_decay_event.h
interface leaves-decay-event {
    use block.{block};
    use types.{host-error};

    resource leaves-decay-event { get-block: func() -> result<block, host-error>; is-cancelled: func() -> result<bool, host-error>; set-cancelled: func(cancelled: bool) -> result<_, host-error>; }
}
