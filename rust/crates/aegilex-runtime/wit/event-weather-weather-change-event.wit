package aegilex:endstone@0.1.0;

// Endstone header: endstone/event/weather/weather_change_event.h
interface weather-change-event {
    use types.{host-error};

    resource weather-change-event { get-to-weather: func() -> result<bool, host-error>; is-cancelled: func() -> result<bool, host-error>; set-cancelled: func(cancelled: bool) -> result<_, host-error>; }
}
