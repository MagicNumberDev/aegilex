package aegilex:endstone@0.1.0;

// Endstone header: endstone/event/block/block_break_event.h
interface block-break-event {
    use block.{block};
    use player.{player};
    use types.{host-error};

    resource block-break-event { get-player: func() -> result<player, host-error>; get-block: func() -> result<block, host-error>; is-cancelled: func() -> result<bool, host-error>; set-cancelled: func(cancelled: bool) -> result<_, host-error>; }
}
