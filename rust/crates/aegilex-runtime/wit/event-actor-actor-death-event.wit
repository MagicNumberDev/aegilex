package aegilex:endstone@0.1.0;

// Endstone header: endstone/event/actor/actor_death_event.h
interface actor-death-event {
    use actor.{actor};
    use types.{host-error};

    resource actor-death-event { get-actor: func() -> result<actor, host-error>; }
}
