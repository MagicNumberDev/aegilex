package aegilex:endstone@0.1.0;

// Endstone header: endstone/event/player/player_interact_actor_event.h
interface player-interact-actor-event {
    use actor.{actor};
    use player.{player};
    use types.{host-error};

    resource player-interact-actor-event { get-player: func() -> result<player, host-error>; get-actor: func() -> result<actor, host-error>; is-cancelled: func() -> result<bool, host-error>; set-cancelled: func(cancelled: bool) -> result<_, host-error>; }
}
