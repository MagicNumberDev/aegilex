package aegilex:endstone@0.1.0;

// Endstone header: endstone/event/actor/actor_spawn_event.h
interface actor-spawn-event {
    use actor.{actor};
    use types.{host-error};

    resource actor-spawn-event { get-actor: func() -> result<actor, host-error>; is-cancelled: func() -> result<bool, host-error>; set-cancelled: func(cancelled: bool) -> result<_, host-error>; }
}
