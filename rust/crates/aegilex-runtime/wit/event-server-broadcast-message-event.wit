package aegilex:endstone@0.1.0;

// Endstone header: endstone/event/server/broadcast_message_event.h
interface broadcast-message-event {
    use message.{message};
    use types.{host-error};

    resource broadcast-message-event { get-message: func() -> result<message, host-error>; set-message: func(message: message) -> result<_, host-error>; is-cancelled: func() -> result<bool, host-error>; set-cancelled: func(cancelled: bool) -> result<_, host-error>; }
}
