package aegilex:endstone@0.1.0;

// Endstone header: endstone/event/player/player_kick_event.h
interface player-kick-event {
    use player.{player};
    use types.{host-error};

    resource player-kick-event { get-player: func() -> result<player, host-error>; get-reason: func() -> result<string, host-error>; set-reason: func(reason: string) -> result<_, host-error>; is-cancelled: func() -> result<bool, host-error>; set-cancelled: func(cancelled: bool) -> result<_, host-error>; }
}
