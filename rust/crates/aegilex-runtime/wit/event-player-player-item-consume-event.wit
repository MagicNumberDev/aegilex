package aegilex:endstone@0.1.0;

// Endstone header: endstone/event/player/player_item_consume_event.h
interface player-item-consume-event {
    use equipment-slot.{equipment-slot};
    use item-stack.{item-stack-ref};
    use player.{player};
    use types.{host-error};

    resource player-item-consume-event { get-player: func() -> result<player, host-error>; get-item: func() -> result<item-stack-ref, host-error>; get-hand: func() -> result<equipment-slot, host-error>; is-cancelled: func() -> result<bool, host-error>; set-cancelled: func(cancelled: bool) -> result<_, host-error>; }
}
