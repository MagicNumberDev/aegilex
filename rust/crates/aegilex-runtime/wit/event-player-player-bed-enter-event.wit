package aegilex:endstone@0.1.0;

// Endstone header: endstone/event/player/player_bed_enter_event.h
interface player-bed-enter-event {
    use block.{block};
    use player.{player};
    use types.{host-error};

    resource player-bed-enter-event { get-player: func() -> result<player, host-error>; get-bed: func() -> result<block, host-error>; is-cancelled: func() -> result<bool, host-error>; set-cancelled: func(cancelled: bool) -> result<_, host-error>; }
}
