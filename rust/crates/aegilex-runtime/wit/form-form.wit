package aegilex:endstone@0.1.0;

// Endstone header: endstone/form/form.h
// Form resource protocol imported by world plugin.
interface form {
    use message.{message};
    use types.{host-error};

    resource form {
        get-title: func() -> result<message, host-error>;
    }
}
