package aegilex:endstone@0.1.0;

// Endstone header: endstone/event/block/block_place_event.h
interface block-place-event {
    use block.{block};
    use player.{player};
    use types.{host-error};

    resource block-place-event { get-player: func() -> result<player, host-error>; get-block-replaced: func() -> result<block, host-error>; get-block-against: func() -> result<block, host-error>; is-cancelled: func() -> result<bool, host-error>; set-cancelled: func(cancelled: bool) -> result<_, host-error>; }
}
