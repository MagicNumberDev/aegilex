package aegilex:endstone@0.1.0;

// Endstone header: endstone/event/player/player_emote_event.h
interface player-emote-event {
    use player.{player};
    use types.{host-error};

    resource player-emote-event { get-player: func() -> result<player, host-error>; get-emote-id: func() -> result<string, host-error>; is-muted: func() -> result<bool, host-error>; set-muted: func(muted: bool) -> result<_, host-error>; is-cancelled: func() -> result<bool, host-error>; set-cancelled: func(cancelled: bool) -> result<_, host-error>; }
}
