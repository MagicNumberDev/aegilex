package aegilex:endstone@0.1.0;

// Endstone header: endstone/event/player/player_pickup_item_event.h
interface player-pickup-item-event {
    use actor.{actor};
    use player.{player};
    use types.{host-error};

    resource player-pickup-item-event { get-player: func() -> result<player, host-error>; get-item-actor: func() -> result<actor, host-error>; is-cancelled: func() -> result<bool, host-error>; set-cancelled: func(cancelled: bool) -> result<_, host-error>; }
}
